!!!
%html{ lang: 'en' }
  %head
    %meta{ charset: 'utf-8' }
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1.0' }

    %title Single Malt

    = stylesheet_link_tag 'application'
    = javascript_include_tag 'vendor/modernizr'
    = csrf_meta_tags

  %body{ class: page_class }
    %section{ role: 'main' }
      %nav.top-bar.show-for-medium-down{ data: { topbar: true} }
        %ul.title-area
          %li.name
            %h1= site_header
          %li.toggle-topbar.menu-icon
            = link_to 'Menu', '#'

        %section.top-bar-section
          %ul.right
            = menu_links(social: true)

      .row
        .large-3.columns.hide-for-medium-down
          .sidebar
            %h3.header= site_header
            %h6.subheader
              All opinions on this page are strictly plagiarized.

            %ul.side-nav
              = menu_links

          - if @last_watched.present?
            .last-watched
              %h5 Last Watched:
              = link_to @last_watched['imdb_url'] do
                = image_tag @last_watched['tvdb_artwork_url']
                %p= plex_episode_name(@last_watched)

          .logo-image-container
            = link_to 'http://www.twitter.com/willmanduffy' do
              = image_tag 'icons/twitter.png'

            = link_to 'http://stackoverflow.com/users/3147094/willmanduffy' do
              = image_tag 'icons/stackoverflow.png'

            = link_to 'http://www.github.com/philduffy' do
              = image_tag 'icons/github.png'

            = link_to 'http://www.rdio.com/people/elementus/' do
              = image_tag 'icons/rdio.png'

            = link_to feed_path do
              = image_tag 'icons/rss.png'

        .large-9.columns.main-container
          = yield

    = javascript_include_tag 'application'
